# ภาพรวมของโปรเจกต์ (Project Overview)
โปรเจกต์ **MAKHRAB** เป็นระบบจัดการข้อมูลและการเช็คชื่อออนไลน์สำหรับ **โรงเรียนไชยฉิมพลีวิทยาคม** 
ระบบถูกออกแบบมาเพื่ออำนวยความสะดวกให้กับทั้งนักเรียนและครู/ผู้ดูแลระบบ โดยมีฟีเจอร์หลักๆ ได้แก่:
*   **สำหรับนักเรียน:** จัดการข้อมูลส่วนตัว, ดูข้อมูลชุมนุม, และตรวจสอบผลการเรียนและสถิติเวลาเรียน
*   **สำหรับครู/ผู้ดูแลระบบ:** เช็คชื่อเข้าเรียนปกติ, เช็คชื่อกิจกรรมชุมนุม, จัดการข้อมูลนักเรียน, ระบบเยี่ยมบ้านนักเรียน (บันทึกพิกัด GPS และรูปถ่าย), ดูสถิติ/รายงาน, และจัดการข้อมูลพื้นฐาน (วิชา, ครู, ชั้นเรียน)
*   **ระบบฐานข้อมูล:** ทำงานร่วมกับ Google Sheets (ผ่าน Google Apps Script) เพื่อบันทึกและซิงค์ข้อมูล

# โครงสร้างโฟลเดอร์และไฟล์ (Folder Structure)
อ้างอิงจากไฟล์ที่มีอยู่ในระบบ โครงสร้างโปรเจกต์ถูกแบ่งออกเป็นสัดส่วน (Modular) ดังนี้:

```text
/
├── index.html                 # หน้า UI หลักของเว็บ (Single Page Application) รวมโครงสร้างหน้าต่างและ Modal ต่างๆ
├── logopng.png                # โลโก้ของโรงเรียน
├── js/                        # โฟลเดอร์เก็บสคริปต์หลัก
│   ├── main.js                # ไฟล์ Entry point สำหรับการทำงานของแอปพลิเคชัน
│   ├── core/                  # โฟลเดอร์เก็บระบบแกนหลัก
│   │   ├── config.js          # ค่าคอนฟิกเริ่มต้น เช่น ชื่อ Key ของฐานข้อมูล
│   │   └── state.js           # จัดการ Global State ของแอปพลิเคชัน (ข้อมูลผู้ใช้, รายชื่อนักเรียน ฯลฯ)
│   ├── utils/                 # โฟลเดอร์เก็บฟังก์ชันช่วยเหลือ (Utilities)
│   │   └── helpers.js         # ฟังก์ชันใช้ซ้ำ เช่น alert, สร้าง ID, จัดรูปแบบวันที่, การบีบอัดรูปภาพ
│   ├── services/              # โฟลเดอร์จัดการการเชื่อมต่อ
│   │   └── api.js             # จัดการการดึง/บันทึกข้อมูลไปยัง LocalStorage และ Google Sheets
│   └── features/              # โฟลเดอร์แยกตามฟีเจอร์การทำงาน
│       ├── students.js        # จัดการข้อมูลนักเรียน (โปรไฟล์ส่วนตัว, เพิ่ม/แก้ไข/ลบ, พอร์ตโฟลิโอวิชาการ)
│       ├── homevisit.js       # จัดการระบบเยี่ยมบ้าน (บันทึกฟอร์ม, พิกัด GPS, จัดการรูปภาพ)
│       └── ...                # (ไฟล์ฟีเจอร์อื่นๆ เช่น จัดการชุมนุม, เช็คชื่อ, สถิติ)
```

# กฎเกณฑ์และสไตล์ไกด์ (Design Guidelines)
เพื่อรักษามาตรฐานความสวยงามและใช้งานง่าย (UX/UI) ของโปรเจกต์ ควรยึดตามหลักการดังนี้:

## 1. ฟอนต์ (Typography)
*   ใช้ฟอนต์หลักคือ **'Sarabun', sans-serif** สำหรับทุกข้อความในระบบ
*   สีตัวอักษรพื้นฐาน: `#1f2937` (Gray-800)

## 2. โทนสีและสไตล์ภาพรวม (Color Palette & Theme)
*   **โทนสีหลัก (Primary):** สีเขียวธรรมชาติ (Green) โทน `green-600` ถึง `green-800`
*   **สไตล์หลัก:** **Glassmorphism (กระจกใสฝ้า)** 
    *   พื้นหลังหลักของเว็บใช้ Animated Gradient: `from-[#e0f2fe] via-[#d1fae5] to-[#ecfdf5]` ผสมกับ Shape เบลอเคลื่อนไหว (Blob)
    *   กล่องข้อความ/พาเนล (`.glass-panel`, `.glass-modal`, `.glass-card`): ใช้ฉากหลังสีขาวแบบโปร่งแสง (opacity ~45-85%), เบลอฉากหลัง `backdrop-filter: blur(16px-24px)`, และมีขอบสีขาวแบบโปร่งแสง

## 3. รูปแบบปุ่ม (Button Styling)
*   **ทุกปุ่มใช้สไตล์ Glassmorphism (ความโปร่งแสง 70%):** 
    *   ใช้คลาส Global CSS override ให้ปุ่มมาตรฐานมีลักษณะนูนลอยสม่ำเสมอกัน
    *   มีความโปร่งแสง 70% (เห็นพื้นหลังเล็กน้อย), ความสูงขั้นต่ำ `44px`, ขอบมน `rounded-xl` (`0.75rem`)
    *   เมื่อนำเมาส์ชี้ (Hover) ให้ปุ่มยกตัวขึ้น `translateY(-2px)` พร้อมเพิ่มเงาให้ดูมีมิติ

## 4. อินพุตและฟอร์ม (Input Controls)
*   ช่องกรอกข้อมูล (Input, Select, Textarea) ใช้สไตล์ `.glass-input`: พื้นหลังโปร่งแสงเล็กน้อย, ขอบสีขาว เมื่อคลิกพิมพ์ (focus) พื้นหลังจะทึบขึ้นเป็น `rgba(255, 255, 255, 0.95)` และมีขอบสีเขียวอ่อนๆ
*   ช่องติ๊กเลือก (Checkbox) ถูกออกแบบใหม่ให้มีขนาดใหญ่ขึ้น เพื่อให้ผู้ใช้งานสามารถคลิกได้ง่ายขึ้น

## 5. การจัดหน้าจอ (Responsive Layout)
*   **Desktop/Tablet:** แสดงผลตารางข้อมูลเต็มรูปแบบ (Table View)
*   **Mobile:** หน้าจอเล็กกว่า 768px จะแปลงตารางเป็นรูปแบบ "การ์ด" (Card View) โดยอัตโนมัติ ซ่อนหัวตารางและแสดงข้อมูลเป็นบล็อกซ้อนกัน เพื่อง่ายต่อการอ่านและสัมผัสบนมือถือ

## 6. คอมโพเนนต์พิเศษ (UI Components)
*   **Toast Notification:** กรอบแจ้งเตือนความสำเร็จ (มุมขวาล่าง) สีเขียว พร้อมแอนิเมชันเลื่อนขึ้น
*   **Modal (Pop-up):** ฉากหลังสีดำโปร่งแสง `backdrop-filter: blur(6px)` ซ้อนด้วยพาเนลกระจก Glassmorphism 
*   **Loading Overlay:** ไอคอนโหลดหมุนๆ พร้อมข้อความแจ้งสถานะแบบลอยทับหน้าจอ

# สรุปการทำงานของ Google Apps Script (Backend API)

## ภาพรวม (Overview)
สคริปต์นี้ทำหน้าที่เป็น **Backend API (Web App)** เพื่อเชื่อมต่อหน้าเว็บ (Frontend) เข้ากับบริการของ Google โดยทำหน้าที่เป็นตัวกลางหลักในการ:
1.  **อ่าน/เขียนฐานข้อมูล:** เชื่อมโยงกับ **Google Sheets** เพื่อใช้เป็นฐานข้อมูล (Database) แทนเซิร์ฟเวอร์ปกติ
2.  **จัดการไฟล์รูปภาพ:** เชื่อมโยงกับ **Google Drive** เพื่อแปลงรูปภาพจาก Base64 (ที่ส่งมาจากหน้าเว็บ) ให้กลายเป็นไฟล์รูปภาพและนำ URL กลับไปบันทึกลงใน Sheets

## ฟังก์ชันทั้งหมด (Functions & Triggers)
*   **`doGet(e)`**: ทำงานเมื่อมีการเรียกผ่าน HTTP GET ทำหน้าที่ดึงข้อมูล (`getData`) จากทุกชีต (เช่น Students, Records, Clubs ฯลฯ) กลับไปเป็นรูปแบบ JSON ให้หน้าเว็บโหลดไปใช้งาน
*   **`doPost(e)`**: ทำงานเมื่อมีการส่งข้อมูล (Submit/Save) ผ่าน HTTP POST โดยจะแยกการทำงานตามค่า `action` ที่ส่งมา เช่น:
    *   `saveStudents`, `saveRecords`, `syncAll`: สำหรับบันทึกข้อมูลแบบเหมาทัังชีต
    *   `saveStudentProfile`: บันทึกประวัติและอัปโหลดรูปภาพนักเรียนรายบุคคล
    *   `getHomeVisitData` / `saveHomeVisitData`: ดึงและบันทึกข้อมูลเยี่ยมบ้าน
*   **`getSheetData(ss, sheetName)`**: ฟังก์ชันช่วยเหลือในการอ่านข้อมูลจาก Sheet แปลงเป็น Array of Objects (จัดการกับฟอร์แมตวันที่ และรหัสที่มีเลข 0 นำหน้าไม่ให้หายไป)
*   **`saveSheetData(ss, sheetName, dataArray)`**: ฟังก์ชันช่วยเหลือในการเขียนข้อมูลทับลงไปใน Sheet พร้อมสร้างคอลัมน์ระบบ (เช่น `createdAt`, `deleted_flg`) ให้โดยอัตโนมัติ
*   **`uploadImageToDrive(base64Data, fileName, mimeType, folderId)`**: รับข้อมูลรูป Base64 ไปสร้างไฟล์ลงใน Google Drive ตามโฟลเดอร์ที่กำหนด (มี 2 โฟลเดอร์คือ เยี่ยมบ้าน และ ข้อมูลนักเรียน) พร้อมเปิดสิทธิ์แชร์และคืนค่าเป็น URL 
*   **`saveStudentsData(ss, studentsArray)`**: สกัดเอาภาพ Base64 ของนักเรียนไปอัปโหลดขึ้น Drive ก่อน แล้วค่อยนำข้อมูล URL ไปบันทึกลงชีต `Students`
*   **`migrateDatabaseForSoftDelete()`**: ฟังก์ชันสำหรับสั่งรันเองแบบ Manual (One-time) เพื่ออัปเดตชีตเก่าทั้งหมดให้รองรับระบบ Soft Delete (เพิ่มคอลัมน์ระบบ)
*   **`forceAuthDrive()`**: ฟังก์ชันสำหรับกดรันครั้งแรก เพื่อบังคับให้ Google ขอสิทธิ์ (Authorization) ในการเข้าถึง Google Drive

## โครงสร้างข้อมูล (Data Mapping)
สคริปต์ทำการจับคู่ (Mapping) ข้อมูลลงชีตต่างๆ ดังนี้:
*   **ชีต `Students`**: ใช้เก็บข้อมูลนักเรียน โดยระบบจะตรวจสอบ Base64 และแปลงเป็น URL เพื่อลงบันทึกในคอลัมน์ `profileImageUrl`, `home_photo_1_url`, `home_photo_2_url`, `home_photo_3_url` โดยอัตโนมัติ 
*   **ชีต `Home_Visits`**: เก็บข้อมูลเยี่ยมบ้าน สร้างขึ้นใหม่ถ้ายังไม่มี มีการ Mapping ข้อมูลจาก Payload ลง 22 คอลัมน์ (เช่น `visit_id`, `student_id`, `visit_date`, สภาพบ้าน ฯลฯ) รวมถึง URL ภาพถ่ายเยี่ยมบ้าน
*   **ชีตอื่นๆ** (`Records`, `Subjects`, `Teachers`, `Classes`, `Clubs`, `ClubEnrollments`, `ClubRecords`): บันทึกข้อมูลแบบ Dynamic (Dynamic Columns) คืออ่าน Header จากแถวแรก ถ้า Payload มี Key ตรงกันก็จะจับคู่ใส่คอลัมน์นั้นให้ หากยังไม่มีคอลัมน์ก็จะสร้าง Header ใหม่ให้ต่อท้าย
*   *หมายเหตุ:* ข้อมูลที่เป็น JSON Array ภายในเซลล์ จะถูก Stringify ตอนบันทึก และ Parse กลับเป็น Array ตอนอ่าน (อ่านค่า `[ ... ]` หรือ `{ ... }`)

## ชุดคำสั่งครั้งต่อไป (Next Prompts)
เพื่อความรวดเร็วในการสั่งงานครั้งถัดไป หากต้องการปรับแก้ Backend คุณสามารถใช้ Prompt ตามรูปแบบนี้ได้:
> *"ฉันต้องการแก้ไข Backend ใน Apps Script ในส่วนของฟังก์ชัน `[ชื่อฟังก์ชัน เช่น saveStudentProfile]` ให้มันสามารถ `[อธิบายสิ่งที่ต้องการ เช่น เช็คคอลัมน์เพิ่มเติม / เปลี่ยนโฟลเดอร์ Drive]` ช่วยเขียนโค้ดสำหรับ Apps Script ให้หน่อย"*
> 
> *"ในส่วนของการรับข้อมูล API ฝั่ง `doPost` ฉันต้องการเพิ่ม action ใหม่ชื่อว่า `[ชื่อแอคชั่น]` เพื่อบันทึกข้อมูลตารางใหม่ ช่วยเขียนฟังก์ชันรองรับใน Apps Script ให้ที"*