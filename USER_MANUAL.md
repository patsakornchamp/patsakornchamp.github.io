# 🏫 คู่มือการใช้งานระบบ MAKHRAB (ระบบจัดการโรงเรียนออนไลน์)
> **เอกสารคู่มือฟังก์ชันการใช้งานระบบสำหรับ ผู้ดูแลระบบ, ครูผู้สอน และนักเรียน**

---

## 📌 สารบัญ (Table of Contents)
1. [ภาพรวมของระบบและสิทธิ์ผู้ใช้งาน](#1-ภาพรวมของระบบและสิทธิ์ผู้ใช้งาน)
2. [คู่มือสำหรับผู้ดูแลระบบ (Admin User Guide)](#2-คู่มือสำหรับผู้ดูแลระบบ-admin-user-guide)
3. [คู่มือสำหรับครูผู้สอน (Teacher User Guide)](#3-คู่มือสำหรับครูผู้สอน-teacher-user-guide)
4. [คู่มือสำหรับนักเรียน (Student User Guide)](#4-คู่มือสำหรับนักเรียน-student-user-guide)
5. [การแก้ไขปัญหาการใช้งานเบื้องต้น (Troubleshooting)](#5-การแก้ไขปัญหาการใช้งานเบื้องต้น-troubleshooting)

---

## 1. ภาพรวมของระบบและสิทธิ์ผู้ใช้งาน

ระบบ **MAKHRAB** เป็นเว็บแอปพลิเคชันหน้าเดียว (Single Page Application) สำหรับบริหารจัดการข้อมูลโรงเรียน การเช็คชื่อ การเยี่ยมบ้าน และการส่งงาน โครงสร้างระบบรองรับทั้งคอมพิวเตอร์ แท็บเล็ต และสมาร์ทโฟน โดยจะปรับเปลี่ยนการแสดงผลให้เหมาะสมกับอุปกรณ์ที่ใช้งานโดยอัตโนมัติ

### 👤 สิทธิ์ผู้ใช้งานในระบบ (User Roles)
ระบบแบ่งประเภทผู้ใช้งานและสิทธิ์การทำธุรกรรมออกเป็น 3 กลุ่มหลัก:

| กลุ่มผู้ใช้งาน | เมนูและหน้าที่หลักในระบบ |
|---|---|
| **ผู้ดูแลระบบ (Admin)** | จัดการข้อมูลดิบพื้นฐานทั้งหมดของโรงเรียน (วิชา, ครู, ห้องเรียน, นักเรียน, ข่าวประชาสัมพันธ์) และการนำเข้าข้อมูล |
| **ครูผู้สอน (Teacher)** | บันทึกการเข้าเรียนรายคาบ, เช็คชื่อชุมนุม, ค้นหาประวัติย้อนหลัง, สรุปสถิติเข้าเรียน (Excel), บันทึกการเยี่ยมบ้านพร้อมเซ็นชื่อและระบุพิกัด, สั่งงานและตรวจการบ้านเด็ก |
| **นักเรียน (Student)** | บันทึกประวัติส่วนตัว/อัปโหลดรูปโปรไฟล์, ลงทะเบียนชุมนุม, ตรวจสอบสถิติเวลาเรียนและคะแนนสะสม, ส่งลิงก์การบ้าน, สแกน QR Code เช็คชื่อ |

---

## 2. คู่มือสำหรับผู้ดูแลระบบ (Admin User Guide)

ผู้ดูแลระบบทำหน้าที่เตรียมและจัดการข้อมูลพื้นฐานผ่านเมนู **"ข้อมูลพื้นฐาน (Master Data)"** ซึ่งแบ่งเป็นแท็บการทำงานย่อยดังนี้:

### 2.1 แท็บ "จัดการวิชา" (Subjects)
*   **การเพิ่มวิชาใหม่:** คลิกปุ่ม `เพิ่มวิชา` -> กรอกรหัสวิชา (เช่น ท21101), ชื่อวิชา และหน่วยกิต -> กด `บันทึก`
*   **การแก้ไข/ลบ:** คลิกไอคอนดินสอสีฟ้าเพื่อแก้ไขข้อมูลวิชา หรือคลิกไอคอนถังขยะสีแดงเพื่อลบวิชาออกจากระบบ

### 2.2 แท็บ "จัดการครูผู้สอน" (Teachers)
*   **การเพิ่มบัญชีครู:** คลิกปุ่ม `เพิ่มครูผู้สอน` -> ระบุข้อมูลส่วนตัว, กำหนดอีเมลและรหัสผ่านเพื่อใช้เข้าสู่ระบบ -> เลือกรายวิชาที่ครูท่านนี้มีสิทธิ์รับผิดชอบสอน -> กด `บันทึก`
*   **การแก้ไขสิทธิ์/วิชา:** คลิกปุ่มแก้ไขเพื่อเปลี่ยนสิทธิ์การสอนของครูแต่ละท่าน

### 2.3 แท็บ "จัดการชั้นเรียน" (Classes)
*   **การตั้งค่าห้องเรียน:** คลิกปุ่ม `เพิ่มชั้นเรียน` -> ระบุปีการศึกษา, ภาคเรียน, ชื่อห้องเรียน (เช่น ม.1/1) -> เลือกครูที่ปรึกษา (สามารถเลือกครูร่วมกันได้หลายคนสำหรับห้องที่ปรึกษาร่วม) -> ติ๊กเลือกวิชาทั้งหมดที่ห้องเรียนนี้ต้องเรียน -> กด `บันทึก`

### 2.4 แท็บ "จัดการนักเรียน" (Students)
*   **เพิ่มนักเรียนรายคน:** คลิกปุ่ม `เพิ่มนักเรียน` -> กรอกประวัตินักเรียน, เลขประจำตัว, เลขประชาชน และประวัติผู้ปกครอง
*   **นำเข้ารายชื่อจำนวนมาก (CSV Import):** คลิกปุ่ม `นำเข้า CSV` -> เลือกไฟล์รายชื่อตามรูปแบบที่กำหนด -> ระบบจะแสดงพรีวิวให้ตรวจสอบข้อมูล -> กดยืนยันบันทึกเพื่อนำเข้าข้อมูลพร้อมกันทั้งชั้นเรียน

### 2.5 แท็บ "จัดการข่าวประชาสัมพันธ์" (PR News)
*   **เพิ่มป้ายข่าวประชาสัมพันธ์:** คลิกปุ่ม `เพิ่มประชาสัมพันธ์` -> ระบุรายละเอียดข่าวสาร, กำหนดวันเริ่มต้นและวันหมดอายุที่ต้องการให้ข่าวแสดงผลบนหน้าแรก -> เลือกอัปโหลดรูปภาพโปสเตอร์หรือระบุ URL รูปภาพ -> กด `บันทึก`
*   **แก้ไขข้อมูลข่าว:** คลิกปุ่มแก้ไขดินสอท้ายแถวข่าว
    *   *ข้อแนะนำ:* หากแก้ไขเฉพาะข้อมูลตัวอักษรหรือขยายวันหมดอายุ **ไม่จำเป็นต้องเลือกอัปโหลดรูปภาพใหม่** ระบบจะใช้รูปภาพเดิมที่เคยอัปโหลดไว้ให้อัตโนมัติ
    *   *การระบุชื่อข่าว:* ระบบไม่บังคับให้กรอกชื่อข่าว หากไม่ได้ระบุ ระบบจะบันทึกเป็นชื่อ "ข่าวประชาสัมพันธ์" ให้โดยอัตโนมัติ

---

## 3. คู่มือสำหรับครูผู้สอน (Teacher User Guide)

ครูผู้สอนเข้าใช้งานฟังก์ชันต่างๆ ผ่านทางเมนูหลักของระบบ ดังนี้:

### 3.1 เมนู "เช็คชื่อปกติ" (Student Attendance)
ใช้ลงบันทึกเวลาเรียนปกติของนักเรียนในวิชาที่สอน:
1.  **เลือกเงื่อนไขการเช็คชื่อ:** เลือก ปีการศึกษา, ภาคเรียน, ชั้นเรียน, วิชา, คาบเรียน (โฮมรูม หรือ คาบ 1-9) และวันที่สอนจริง
2.  **ดึงรายชื่อนักเรียน:** คลิกปุ่ม `โหลดรายชื่อ` เพื่อแสดงรายการรายบุคคล
3.  **การบันทึกสถานะ:**
    *   **วิธีบันทึกด่วน:** คลิกปุ่ม `เช็คทั้งหมดเป็น มา` เพื่อเปลี่ยนทุกคนในห้องเรียนเป็นสถานะมาเรียน จากนั้นค่อยติ๊กเลือกเปลี่ยนเฉพาะบางคนเป็น สาย, ลา หรือ ขาด
    *   **วิธีบันทึกรายคน:** คลิกเลือกปุ่มสถานะสีของนักเรียนแต่ละคน: **`มา` (เขียว) | `สาย` (เหลือง) | `ลา` (น้ำเงิน) | `ขาด` (แดง)**
    *   สามารถติ๊กถูกที่ช่อง `แสดงเฉพาะคนที่ยังไม่เช็คชื่อ` เพื่อช่วยให้เช็คเฉพาะนักเรียนที่ตกหล่นได้ง่ายขึ้น
4.  **ระบบเช็คชื่อผ่าน QR Code:**
    *   **ปุ่ม `โชว์ QR`:** คลิกเพื่อให้ระบบสร้าง QR Code ประจำคาบเรียนขึ้นหน้าจอ ให้นักเรียนเปิดแอปในมือถือสแกนเช็คชื่อด้วยตัวเอง (QR Code จะเปลี่ยนสถานะทุกๆ 5 นาทีเพื่อป้องกันการนำไปสแกนนอกชั้นเรียน)
    *   **ปุ่ม `สแกนเด็ก`:** เปิดกล้องบนมือถือของคุณครูเพื่อสแกน QR Code ประจำตัวนักเรียนที่เปิดโชว์บนโทรศัพท์ของตัวนักเรียน
    *   **ปุ่ม `ดึงสแกน`:** กดยืนยันเพื่อดึงยอดและบันทึกประวัติการสแกน QR ของเด็กทุกคนเข้ามาลงตารางสรุปโดยอัตโนมัติ
5.  คลิกปุ่ม **`บันทึกข้อมูลการเช็คชื่อ`** ด้านล่างสุดเพื่อส่งข้อมูลขึ้นระบบคลาวด์

### 3.2 เมนู "เช็คชื่อชุมนุม" (Club Attendance)
ใช้บันทึกเวลาเรียนประจำวันของนักเรียนในกิจกรรมชุมนุมพัฒนาผู้เรียน:
1.  เลือกปีการศึกษา, ภาคเรียน, เลือกกิจกรรมชุมนุมที่รับผิดชอบ และกำหนดวันที่จัดกิจกรรม
2.  คลิกปุ่ม `โหลดรายชื่อ` เพื่อแสดงผลรายชื่อเด็กที่สมัครเข้าร่วมชุมนุมนี้
3.  ติ๊กสถานะผู้เรียนและกดคลิกปุ่ม **`บันทึกเช็คชื่อกิจกรรมชุมนุม`**

### 3.3 เมนู "ประวัติเข้าเรียน" (Attendance History)
*   ใช้สำหรับตรวจเช็คข้อมูลเช็คชื่อย้อนหลัง เลือกกรองตามประเภท (ปกติ/ชุมนุม), วันที่ และชั้นเรียน
*   **ดูข้อมูลละเอียด:** คลิกไอคอนดวงตาเพื่อตรวจสอบสถานะรายบุคคลในคาบนั้น
*   **แก้ไขย้อนหลัง:** คลิกไอคอนดินสอเพื่อแก้ไขสถานะของนักเรียนย้อนหลังหากเกิดข้อผิดพลาด
*   **ลบประวัติคาบ:** คลิกไอคอนถังขยะหากต้องการลบประวัติการเช็คชื่อของคาบนั้นออก

### 3.4 เมนู "สถิติ" (Attendance Statistics)
*   เลือกชั้นเรียนและรายวิชาที่ต้องการเรียกรายงาน
*   ระบบจะสรุปจำนวนการ มา, สาย, ลา, ขาด และวิเคราะห์เป็น **เปอร์เซ็นต์เวลาเรียนเข้าเรียน (%)** 
*   *คำเตือน:* หากเด็กมีเปอร์เซ็นต์เวลาเรียนต่ำกว่า 80% ป้ายแสดงผลจะเป็นแถบสีแดงเพื่อเตือนสถานะความเสี่ยงไม่มีสิทธิ์เข้าสอบ
*   **การดาวน์โหลดข้อมูล:** คลิกปุ่ม `ส่งออกข้อมูล (Export to Excel)` เพื่อบันทึกเป็นไฟล์เอกสารสรุปเวลาเรียนสำหรับนำไปพิมพ์ต่อ

### 3.5 เมนู "เยี่ยมบ้าน" (Home Visit Portal)
เครื่องมือบันทึกและจัดการข้อมูลการลงพื้นที่เยี่ยมบ้านของครูที่ปรึกษา:
1.  เลือกชั้นเรียนที่ท่านเป็นที่ปรึกษา และกดปุ่ม `ค้นหา`
2.  ระบบจะแสดงรายชื่อนักเรียนทั้งหมด
    *   **ตรวจสอบเบื้องต้น:** คลิกปุ่ม `พิกัด/รูปบ้าน` สีน้ำเงินเพื่อดูรูปถ่ายบ้าน 3 รูปและแผนที่ตั้งที่นักเรียนได้อัปโหลดไว้ล่วงหน้า
    *   **อัปเดตสถานะเร็ว:** เลือกเปลี่ยนแถบดรอปดาวน์สถานะการเยี่ยมบ้านได้ทันที (สำเร็จ, ยังไม่เยี่ยม, เลื่อนนัด, ไม่พบผู้ปกครอง)
3.  **ขั้นตอนการกรอกฟอร์มเยี่ยมบ้านแบบละเอียด (5 ขั้นตอน):**
    *   คลิกปุ่ม `บันทึก` (ปุ่มสีฟ้าท้ายรายชื่อเด็ก)
    *   **ขั้นตอนที่ 1-3 (ข้อมูลครอบครัวและสภาพบ้าน):** กรอกสภาพการเดินทาง, พฤติกรรม, ปัญหาเฝ้าระวัง และข้อเสนอแนะ 
        *   *ปุ่มทางลัด:* กดปุ่ม `คัดลอกข้อมูลผู้ปกครองจากฐานข้อมูล` เพื่อดึงข้อมูลชื่อ-เบอร์โทรพ่อแม่มาลงฟอร์มได้ทันที
    *   **ขั้นตอนที่ 4 (ระบุพิกัดและรูปภาพจริง):** คุณครูอัปโหลดรูปถ่ายขณะครูเยี่ยมบ้านนักเรียน
        *   *จับพิกัด GPS:* เมื่อยืนอยู่ที่ตั้งหน้าบ้านเด็กจริง ให้ครูกดปุ่ม **`ดึงพิกัดปัจจุบัน`** บนมือถือ ระบบจะดึงพิกัดละติจูด/ลองจิจูดจากดาวเทียมลงฟอร์มอัตโนมัติ
    *   **ขั้นตอนที่ 5 (ลงลายมือชื่อ):** ส่งโทรศัพท์ให้นักเรียนหรือผู้ปกครองใช้นิ้วเซ็นลงชื่อกำกับบนหน้าจอมือถือครู (หากเซ็นผิดพลาดให้กดปุ่ม `ล้างลายมือชื่อ` เพื่อเขียนใหม่)
4.  กด **`บันทึกข้อมูลการเยี่ยมบ้าน`** เพื่อเสร็จสิ้นขั้นตอน 
5.  คลิกปุ่ม **`รายงาน` (ปุ่มสีเขียว)** เพื่อพรีวิวหน้าเอกสารรายงานเยี่ยมบ้านขนาด A4 และสั่งพิมพ์ออกทางเครื่องพิมพ์ได้ทันที

### 3.6 เมนู "งานและคะแนน" (Assignments)
ฟังก์ชันสั่งการบ้านและบันทึกผลคะแนนรายวิชา:
1.  **สั่งงานชิ้นใหม่:** คลิกปุ่ม `มอบหมายงานใหม่` -> เลือกห้องเรียน, เลือกวิชา, กรอกรายละเอียดและเกณฑ์ของงาน, กำหนดคะแนนเต็ม และวันกำหนดส่งงาน -> กด `สร้างงานมอบหมาย`
2.  **ตรวจคะแนนและผลงานนักเรียน:** คลิกปุ่ม `ดูการส่งงาน` (ท้ายชิ้นงานนั้นๆ) -> คลิกปุ่ม `ตรวจงาน` ในรายชื่อนักเรียนที่ส่งผลงานแล้วเพื่อเปิดดูไฟล์หรือลิงก์งาน -> กรอกคะแนนจริงที่เด็กได้รับพร้อมระบุความคิดเห็นครูผู้สอน -> กดบันทึกคะแนน
3.  **การลบงานที่สั่ง:** คลิกไอคอนถังขยะสีแดงท้ายงานชิ้นนั้น
    *   *กฎการลบ:* คุณครูจะ**ลบชิ้นงานได้ต่อเมื่อยังไม่มีนักเรียนคนใดส่งงานเข้ามา และครูยังไม่ได้บันทึกคะแนนให้ใครในงานนั้นเท่านั้น** หากมีข้อมูลการส่งงานหรือคะแนนแล้ว ระบบจะล็อกเพื่อป้องกันการสูญเสียผลงานนักเรียน
4.  **การส่งออกรายงานคะแนนรวม (Export Excel):**
    *   *วิธีดาวน์โหลดไฟล์ Excel:* คุณครูต้องระบุตัวเลือก **"กรองตามชั้นเรียน"** และ **"กรองตามวิชา"** ในหน้านี้ให้ครบถ้วนก่อน ปุ่มดาวน์โหลดสีเขียว `ส่งออกรายงานคะแนน (Excel)` จึงจะปรากฏขึ้นมาทางมุมขวาบนให้คุณครูใช้งาน

---

## 4. คู่มือสำหรับนักเรียน (Student User Guide)

นักเรียนเข้าใช้งานฟังก์ชันต่างๆ ผ่านทางเมนูบนอุปกรณ์ของตนเอง ดังนี้:

### 4.1 เมนู "ข้อมูลส่วนตัว" (My Profile)
*   **อัปโหลดรูปประจำตัว:** คลิกปุ่มไอคอนรูปกล้องถ่ายภาพด้านล่างรูปโปรไฟล์เพื่อเปิดกล้องมือถือถ่ายภาพตัวเองหน้าตรง หรือเลือกรูปถ่ายจากคลังภาพเพื่ออัปโหลดเข้าระบบ
*   **อัปเดตประวัติ:** แก้ไขเบอร์โทรศัพท์ส่วนตัว, ข้อมูลบิดา-มารดา, ประวัติผู้ปกครอง และพิกัดบ้านของตนเอง จากนั้นคลิกปุ่ม **`บันทึกข้อมูลของฉัน`**

### 4.2 เมนู "ชุมนุมของฉัน" (My Club)
*   **ลงทะเบียนชุมนุม:** ระบบจะแสดงรายการชุมนุมทั้งหมดพร้อมจำนวนโควตาที่เปิดรับ นักเรียนสามารถกดคลิกปุ่ม **`ลงทะเบียนเข้าร่วม`** ในชุมนุมที่สนใจและยังไม่เต็มได้
*   **ตรวจสอบเวลาเรียน:** ด้านล่างของหน้านี้จะแสดงสรุปจำนวนประวัติการเช็คชื่อเข้ากิจกรรมชุมนุมย้อนหลังของนักเรียน

### 4.3 เมนู "ผลการเรียน & เวลาเรียน" (Academic Portal)
*   หน้าต่างแสดงผลการเรียนและคะแนนเก็บทุกรายวิชา
*   แสดงผลการเข้าเรียนรวมปกติ (จำนวนชั่วโมงที่เข้าเรียนย้อนหลังแยกเป็น มา, สาย, ลา, ขาด ของแต่ละวิชา) เพื่อให้นักเรียนตรวจสอบเวลาเรียนของตนเองไม่ให้ขาดเรียนจนหมดสิทธิ์สอบ

### 4.4 เมนู "การบ้านและงาน" (Student Assignments)
1.  **ดูการบ้านค้างส่ง:** ตรวจสอบชื่องาน คะแนนเต็ม และกำหนดวันส่งงานที่คุณครูสั่งมอบหมาย
2.  **การส่งงาน:**
    *   คลิกปุ่ม **`ส่งงาน`** ท้ายการบ้านชิ้นนั้น
    *   กรอก URL ลิงก์ที่เก็บไฟล์ผลงานจริง (เช่น ลิงก์ไฟล์งานใน Google Drive, ลิงก์วิดีโอนำเสนอใน YouTube, ลิงก์รูปภาพ หรือผลงานนำเสนอใน Canva)
    *   พิมพ์ข้อความอธิบายการส่งสั้นๆ ถึงคุณครู (ถ้ามี) -> กด `ส่งงาน`
    *   สถานะงานจะเปลี่ยนเป็นสีน้ำเงิน **"ส่งแล้ว"** 
3.  **เช็คคะแนนที่ได้:** เมื่องานได้รับการประเมินจากครูแล้ว สถานะจะอัปเดตเป็นสีเขียว **"ตรวจแล้ว"** พร้อมแสดงคะแนนที่ได้รับและความเห็นแนะแนวจากคุณครูบนหน้าต่างนี้

### 4.5 เมนู "สแกน QR Code" (Student QR Attendance)
ระบบบันทึกเวลาเรียนปกติของนักเรียนด้วยตนเองผ่านมือถือ:
*   **โหมดสแกนส่องกล้อง:** เมื่อคุณครูฉายภาพ QR Code ประจำคาบเรียนไว้หน้าห้องเรียน ให้นักเรียนคลิกปุ่ม **`สแกน QR ห้องเรียน`** บนมือถือของตัวเองเพื่อสแกน (ระบบจะลงเวลาเรียนให้ทันทีโดยอัตโนมัติ โดยมีการเช็คว่าตัวนักเรียนอยู่ในขอบเขตบริเวณโรงเรียนผ่านพิกัด GPS จริง)
*   **โหมดแสดงรหัสตัวเอง:** แตะปุ่ม **`แสดง QR ของฉัน`** เพื่อโชว์ภาพรหัสประจำตัวของตัวเองให้นักเรียนคุณครูนำเครื่องมาสแกนเช็คชื่อให้แทน

---

## 5. การแก้ไขปัญหาการใช้งานเบื้องต้น (Troubleshooting)

### 5.1 ระบบขอสิทธิ์เข้าถึงพิกัดตำแหน่ง (GPS Location)
*   **อาการ:** หน้าจอค้าง แจ้งเตือนไม่พบพิกัด หรือดึงละติจูดลองจิจูดล้มเหลวตอนเช็คชื่อ QR Code หรือกรอกฟอร์มเยี่ยมบ้าน
*   **วิธีแก้ไข:** ตรวจสอบว่าได้กดยินยอม (Allow) ให้เบราว์เซอร์บนโทรศัพท์มือถือเข้าถึงสิทธิ์ตำแหน่งพิกัดของอุปกรณ์ (Location Access) หรือยัง โดยหากเผลอกดบล็อกไว้ ให้เข้าไปที่ตั้งค่าของเบราว์เซอร์ -> ตั้งค่าเว็บไซต์ -> สิทธิ์เข้าถึงตำแหน่ง -> กดเลือกอนุญาตให้กับชื่อเว็บของระบบ

### 5.2 ปุ่มส่งออก Excel (Export Report) ไม่แสดงขึ้นมา
*   **อาการ:** ครูต้องการโหลดสรุปคะแนนงานนักเรียนทั้งห้อง แต่อ่านเมนูการบ้านแล้วไม่พบปุ่มดาวน์โหลดรายงาน
*   **วิธีแก้ไข:** ตรวจสอบให้แน่ใจว่าคุณครูได้กดเลือกตัวกรอง **"กรองตามชั้นเรียน"** และ **"กรองตามวิชา"** ในหน้าจัดการงานเรียบร้อยแล้ว ปุ่มสีเขียวสำหรับกดส่งออกรายงานจึงจะแสดงขึ้นมาโดยอัตโนมัติ

### 5.3 กดลบงานที่สั่งมอบหมายไปแล้วไม่ได้
*   **อาการ:** ระบบแสดงข้อความแจ้งเตือน "ไม่สามารถลบงานนี้ได้ เนื่องจากมีการส่งงานหรือคุณได้ให้คะแนนนักเรียนไปแล้ว"
*   **วิธีแก้ไข:** เป็นมาตรการความปลอดภัยเพื่อรักษาผลงานของเด็ก หากมีนักเรียนที่ส่งงานเข้ามาแล้ว (แม้สถานะจะเป็น 'ส่งแล้ว' หรือ 'ตรวจแล้ว' หรือมีคะแนนกรอกไว้แม้แต่คนเดียว) ระบบจะไม่ให้ลบงานชิ้นนั้น หากจำเป็นต้องลบจริงๆ ครูผู้สอนต้องเข้าไปยกเลิกประวัติการส่งงาน/ลบคะแนนของเด็กคนนั้นออกให้หมดก่อน จึงจะกดลบชิ้นงานได้
